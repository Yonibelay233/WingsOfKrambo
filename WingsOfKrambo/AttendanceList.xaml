<Page x:Class="WingsOfKrambo.AttendanceList"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WingsOfKrambo"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="AttendanceList">

    <Grid>
        <DataGrid x:Name="dgApprentices" AutoGenerateColumns="False" FlowDirection="RightToLeft" Margin="20">
            <DataGrid.Columns>
                <DataGridTextColumn Header="שם פרטי" Binding="{Binding FirstName}" Width="*" IsReadOnly="True"/>
                <DataGridTextColumn Header="שם משפחה" Binding="{Binding LastName}" Width="*" IsReadOnly="True"/>
                <DataGridTextColumn Header="תעודת זהות" Binding="{Binding ID}" Width="*" IsReadOnly="True"/>

                <DataGridTextColumn Header="נוכחות" Binding="{Binding AttendanceStatus}" Width="150" IsReadOnly="True" />
            </DataGrid.Columns>
        </DataGrid>

    </Grid>
</Page>
