<Page x:Class="WingsOfKrambo.ApprenticeAction"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="800" d:DesignWidth="1200"
      Title="ApprenticeAction"
      FlowDirection="RightToLeft"
      Background="#F5F7FA">

    <Page.Resources>
        <Style x:Key="FieldLabelStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#1A3A5C"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
        </Style>

        <Style x:Key="ModernTextBoxStyle" TargetType="TextBox">
            <Setter Property="Height" Value="40"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E0E6ED"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <Style x:Key="ModernComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Height" Value="40"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E0E6ED"/>
            <Setter Property="Padding" Value="10,5"/>
        </Style>

        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#1A3A5C"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="24,12"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
    </Page.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="0,0,0,40">
            <Grid Height="120" Background="#1A3A5C">
                <StackPanel VerticalAlignment="Center" Margin="48,0">
                    <TextBlock Text="טופס רישום פעילות" FontSize="28" FontWeight="Bold" Foreground="White"/>
                    <TextBlock Text="כנפיים של קרמבו - סניף מקומי" FontSize="16" Foreground="#A0B3C6" Margin="0,4,0,0"/>
                </StackPanel>
            </Grid>

            <Border Margin="48,36,48,24" Background="White" CornerRadius="16" Padding="48,40">
                <Border.Effect>
                    <DropShadowEffect Color="#1A3A5C" BlurRadius="30" ShadowDepth="0" Opacity="0.08"/>
                </Border.Effect>

                <StackPanel>
                    <TextBlock Text="פרטי הפעולה" FontSize="18" FontWeight="Bold" Foreground="#1A3A5C"/>
                    <Separator Background="#EEE8DC" Margin="0,8,0,24"/>

                    <StackPanel Margin="0,0,0,24">
                        <TextBlock Text="תאריך הפעולה (יום שלישי הקרוב)" Style="{StaticResource FieldLabelStyle}"/>
                        <TextBox x:Name="TxtAutoDate" Style="{StaticResource ModernTextBoxStyle}" IsReadOnly="True" Background="#F5F5F5" Margin="0,0,0,16"/>

                        <TextBlock Text="האם אתה מגיע לפעילות?" Style="{StaticResource FieldLabelStyle}"/>
                        <StackPanel Orientation="Horizontal">
                            <RadioButton x:Name="RbComing" Content="כן, אני מגיע/ה" IsChecked="True" Margin="0,0,20,0"/>
                            <RadioButton x:Name="RbNotComing" Content="לא, לא אוכל להגיע"/>
                        </StackPanel>
                    </StackPanel>

                    <Button x:Name="BtnSubmit" 
                            Content="שלח" 
                            Style="{StaticResource PrimaryButtonStyle}" 
                            HorizontalAlignment="Left" 
                            Margin="0,40,0,0"
                            Click="BtnSubmit_Click"/>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</Page>